import { 
  Loader2, 
  AlertCircle, 
  CheckCircle2, 
  XCircle, 
  Info,
  Image as ImageIcon
} from 'lucide-react';

export const Icons = {
  spinner: Loader2,
  alert: AlertCircle,
  check: CheckCircle2,
  close: XCircle,
  info: Info,
  logo: ImageIcon, // Using ImageIcon as a fallback for the logo
};

export default Icons;
